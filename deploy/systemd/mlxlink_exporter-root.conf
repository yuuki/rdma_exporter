[Service]
# Use only when the MFT command has been verified to require a root identity.
# This restores the normal capability bounding set while inheriting the
# filesystem hardening from mlxlink_exporter.service.
User=root
Group=root
CapabilityBoundingSet=~
